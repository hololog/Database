<데이터정의어(DDL)> => 작업단위 : object(table)
create table ---;
alter table ---;
drop table ---;
truncate table ---;

<데이터조작어(DML)> => 작업단위 : Data(table내 data)
select ---;
insert ---;
update ---;
delete ---;

<데이터제어어(DCL)> => 작업단위 : User의 권한
grant ---;
revoke ---;